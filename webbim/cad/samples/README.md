# Sample models

`hillside-town.ocaf.json` is the document the **Samples → Hillside town** button
loads. It is here so it can be read as text; in the artifact, press the button.

47 features, no script. See **Samples** in `../README.md` for what each part of
the graph does.
